# Copy this directory for a local task; replace paths and task parameters.
# Caller supplies REPO_ROOT from its own script location.
export YAM_TRAIN_ROOT=${YAM_TRAIN_ROOT:-${REPO_ROOT}/data/training}
