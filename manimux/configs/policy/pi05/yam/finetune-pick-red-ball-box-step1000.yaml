# Locally trained Pi05-YAM checkpoint for the red-ball-to-box task.
policy_name: Pi_05
protocol: ws
host: 127.0.0.1
port: 8500

task_name: pick_the_red_ball_up_and_place_it_into_the_box
env_cfg_type: yam_dual
action_type: joint
eval_batch: false
checkpoint_variant: pi05_yam_pick_red_ball_box_step_1000

train_config_name: pi05_yam
observation_profile: yam_native
repo_id: yam_pick_red_ball_box_v1
model_path: finetuned/ziyang/pi05-yam-pick-red-ball-box-b384/1000
checkpoint_source: local_pi05_base_finetune_step_1000
norm_stats_path: finetuned/ziyang/pi05-yam-pick-red-ball-box-b384/1000/assets/yam_pick_red_ball_box_v1
norm_stats_source: checkpoint_matched_yam_pick_red_ball_box_v1
use_delta_joint_actions: true

action_horizon: 50
num_steps: 10

backend_identity:
  fields: [task_name, checkpoint_variant, checkpoint_source, norm_stats_path, norm_stats_source, train_config_name, action_horizon, num_steps]
  aliases: {model_root: model_path}
  model: {policy_family: pi05}
